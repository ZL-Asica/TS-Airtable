# FieldSet

> **FieldSet** = [`AirtableFieldSet`](../Interfaces/AirtableFieldSet.md)

Defined in: [types/index.ts:67](https://github.com/ZL-Asica/TS-Airtable/blob/v0.5.0/types/index.ts#L67)

Compatibility aliases matching the official `airtable` package.

These names are kept short on purpose so that migrating code can often
switch from:

  import { FieldSet, Record } from 'airtable'

to:

  import { FieldSet, Record } from 'ts-airtable'

with minimal changes.
