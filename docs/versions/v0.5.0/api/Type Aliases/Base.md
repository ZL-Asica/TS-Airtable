# Base

> **Base** = [`AirtableBase`](../Interfaces/AirtableBase.md)

Defined in: [types/index.ts:43](https://github.com/ZL-Asica/TS-Airtable/blob/v0.5.0/types/index.ts#L43)

Short alias for [AirtableBase](../Interfaces/AirtableBase.md), matching the official `airtable` type name.

In most new code we recommend using `AirtableBase`, but `Base` is provided
for easier migration from `airtable`.
