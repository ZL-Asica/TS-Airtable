# Error

> **Error** = [`AirtableError`](../Classes/AirtableError.md)

Defined in: [types/index.ts:50](https://github.com/ZL-Asica/TS-Airtable/blob/v0.4.2-beta.0/types/index.ts#L50)

Short alias for [AirtableError](../Classes/AirtableError.md), matching the official `airtable` type name.

Note: this shadows the global `Error` type only within the `ts-airtable`
import namespace. It does not modify the global `Error`.
