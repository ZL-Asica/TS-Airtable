# AirtableViewMetadata

> **AirtableViewMetadata** = [`AirtableViewSchema`](../Interfaces/AirtableViewSchema.md)

Defined in: [types/metadata.ts:190](https://github.com/ZL-Asica/TS-Airtable/blob/v0.4.2/types/metadata.ts#L190)

Metadata for a single view, as returned by **"Get view metadata"**.

In practice this has the same core shape as [AirtableViewSchema](../Interfaces/AirtableViewSchema.md),
but may include additional fields depending on the view type.
